/**
 * js/app.jsx  (loaded by Babel standalone — must be last script)
 * Entry point: mounts the React app into #root.
 */
ReactDOM.createRoot(document.getElementById('root')).render(
  React.createElement(window.MultiSphereSimulation)
);
